recuperer le chemin de le debut.
fonction pour free + Fonction strjoinfree
list2.c fonction back (free chaque element avant de free le maillon)
list3.c remplire les maillon
checker si c'est un fichier ou un dossier
"ft_ls -" => segfault
si on as pas la permission 
//si getpuid n'accorde pas le droit juste ecrire l'id
les liens
le total


Ce qui est fait


le checker d'arguments
le trie de argv et la liste
option a
option l {sauf lien et le total}
option r
option t

bonus 
trie par size
afficher que les directory



/*void g_debug()
{
	printf("----OPTION-----\n");
	if (g_bit & OPTION_a)
		ft_putendl("a");
	if (g_bit & OPTION_l)
		printf("l\n");
	if (g_bit & OPTION_r)
		printf("r\n");
	if (g_bit & OPTION_R)
		printf("R\n");
	if (g_bit & OPTION_t)
		printf("t\n");
	if (g_bit & OPTION_STOP)
		printf("--\n");
}*/