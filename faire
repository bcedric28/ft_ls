Fonction pour trier par date // voir le temps avec plus de six mois et moins que si mois OK
Fonction pour swap a ameliorer OK
Fonction pour parcourir la sous liste chainer et ouvrir les documents pour cree nouvelle liste EN cour 
fonction pour free + Fonction strjoinfree
fonction afficher


/*

if (((g_bit & 4) && !(g_bit & 16)) ? 
		ft_strcmp(tab[i], tab[i + 1]) < 0 :

*/